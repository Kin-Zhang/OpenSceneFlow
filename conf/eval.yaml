
dataset_path: /home/ssd/qingwen/data/av2/h5py/demo/
checkpoint: 
data_mode: val # [val, test]
save_res: False # [True, False]

leaderboard_version: 1 # [1, 2]
supervised_flag: True # [True, False], whether you use any label from the dataset

# no need to change
save_res_path: 
num_frames: 4
flow_num: 2 # default is 1; pc0->pc1; flow_num=3 means we will have pc1 -> pc2 -> pc3
slurm_id: 00000
output: ${model.name}-${slurm_id}
gpus: 1
seed: 42069
eval_only: True
wandb_mode: disabled # [offline, disabled, online]
defaults:
  - hydra: default
  - model: floxels